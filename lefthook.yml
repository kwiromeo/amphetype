pre-commit:
  commands:
    ty:
      glob: "*.py"
      run: ty check {staged_files}
    ruff:
      glob: "*.py"
      run: ruff check {staged_files}
pre-push:
  commands:
    ruff-format:
      glob: "*.py"
      run: ruff format --check {staged_files}
